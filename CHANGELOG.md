# Changelog

All notable changes to `etc-cli` should be recorded here.

## Unreleased

- Extracted the CLI module as a standalone Zig package.
- Added parser expansion for `--flag=value`, explicit bool values, negation, and
  short flag bundles.
- Added generated help, shell completion, man page, command schema, and package
  artifact helpers.
- Added validation, snapshot, lint, and parser property coverage.
