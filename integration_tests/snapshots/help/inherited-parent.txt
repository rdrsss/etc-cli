group

  Parent command

USAGE:
  group [flags] <command>

COMMANDS:
  run             Leaf command

FLAGS:
  --global, -g          (bool) default=false — Global flag
  --profile             (string) — Profile name

FLAG GROUPS:
  group-scope         at least one required: --global, --profile — Choose a global or profile scope.
