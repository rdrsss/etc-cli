tool

  Snapshot tool

USAGE:
  tool [flags] <command>

COMMANDS:
  run             Run target

FLAGS:
  --verbose, -v         (bool) default=false — Verbose output
  --color               (auto|always|never) default="auto" — When to colorize output

FLAG GROUPS:
  display             at least one required: --verbose, --color — Choose at least one display control.
